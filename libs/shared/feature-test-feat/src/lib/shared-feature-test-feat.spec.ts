import { sharedFeatureTestFeat } from './shared-feature-test-feat';

describe('sharedFeatureTestFeat', () => {
  it('should work', () => {
    expect(sharedFeatureTestFeat()).toEqual('shared-feature-test-feat');
  });
});
